import { useEffect, useState } from "react";
import { IAnimals } from "../models/IAnimals";
import { Animal } from "./animal";

export const Animaltrueorfalse = () => {
  const [food, setFood] = useState<IAnimals[]>([]);

  return <h1></h1>;
};
